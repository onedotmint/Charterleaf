# Project Constitution

> Keep only a few project-wide guardrails that are already established facts or explicit decisions. Do not fill this file merely because the template exists.

## Principles

<!--
Add only rules that apply across the project and should rarely change.

Examples of the *kind* of rule that may belong here, only if true for this project:
- Preserve public API compatibility unless an explicit contract change is intended.
- Follow existing architectural boundaries before introducing new ones.
- New dependencies require a concrete project benefit.
- Behavior changes require relevant verification.

Keep coding-style details, task state, temporary constraints, and implementation notes elsewhere.
-->
